[package]
name = "debos-vfs"
version.workspace = true
edition.workspace = true
description = "DebOS Virtual Filesystem Server"

[dependencies]
libdebos = { path = "../../libdebos" }

# Required for no_std allocator
[features]
default = []
